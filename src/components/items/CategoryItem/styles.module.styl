@require '../../../layout/mixins/palette';

.name {
  font-size: 18px;
  font-weight: 500;
}

.img {
  position: absolute;
  width: 100%;
  height: 100%;
  top: 0px;
  object-fit: cover;
  display: block;
}